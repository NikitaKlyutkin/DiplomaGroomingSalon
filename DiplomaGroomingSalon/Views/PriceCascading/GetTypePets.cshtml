@model IEnumerable<DiplomaGroomingSalon.Domain.Entities.PetType>

@{
	ViewData["Title"] = "GetTypePets";
}

<h1>GetServiceTypes</h1>
<p>
	<a asp-action="CreateTypePet">Create New PetType</a>
</p>
<p>
	<a asp-action="CreateBreedPet">Create New Breed</a>
</p>
<p>
	<a asp-action="CreateServiceType">Create New ServiceType</a>
</p>
@if (Model == null)
{
	<div class="col-md-8">
		<div class="card-body">
			<h5 class="card-title text-center">Список пуст :(</h5>
		</div>
	</div>
}
else
{
	<table class="table">
		<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Id)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.typePetName)
			</th>
			<th></th>
		</tr>
		</thead>
		<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.typePetName)
				</td>
				<td>
					@Html.ActionLink("Edit", "EditTypePet", new {id = item.Id}) |
						@Html.ActionLink("Delete All(including PetType)", "DeleteTypePets", new {id = item.Id})
				</td>
			</tr>
		}
		</tbody>
	</table>
}